# Environment files (ignore at root, client, server, and all subdirectories)
.env
.env.*
**/.env
**/.env.*

# Node modules (ignore at root, client, server, and all subdirectories)
node_modules/
**/node_modules/


